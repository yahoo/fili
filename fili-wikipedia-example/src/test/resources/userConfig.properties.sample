# Don't delete, use for testing!
test__sample_default_config = default-config

# Use memory by default in the tests locally
test__dimension_backend = memory

test__druid_cache_enabled = true
test__druid_cache_v2_enabled = true

test__druid_coordinator_metadata_enabled = true

# Default the timeout to 10 minutes, in milliseconds
test__druid_request_timeout = 600000
